# Jojo OS website — PUBLIC placeholders only. Never put real tokens here.
# The current landing page is plain static HTML and does not read these; edit the
# GitHub URL directly in index.html. These are documented for a future framework build.
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/your-username/jojo-os
NEXT_PUBLIC_APP_NAME=Jojo OS
