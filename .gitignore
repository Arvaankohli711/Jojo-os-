node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# build/ holds electron-builder resources (app icon + entitlements) — ship those,
# not build output. Ignore the dir contents but keep the branding assets.
build/*
!build/icon.png
!build/icon.ico
!build/icon.icns
!build/entitlements.mac.plist

# Jojo OS — never commit private user data or secrets
JojoUserData/
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets/
tokens/
logs/
exports/private/
vault/private/
local_models/
